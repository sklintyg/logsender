ACTIVEMQ_BROKER_URL=${broker.amq.tcp.port}?jms.nonBlockingRedelivery=true&jms.redeliveryPolicy.maximumRedeliveries=5&jms.redeliveryPolicy.maximumRedeliveryDelay=20000&jms.redeliveryPolicy.initialRedeliveryDelay=15000&jms.redeliveryPolicy.useExponentialBackOff=true&jms.redeliveryPolicy.backOffMultiplier=2
ENV_NAME=test
LOGGTJANST_LOGICALADDRESS=SE000000000000-0000
NTJP_BASE_URL=http://localhost:8080/stubs
REDIS_HOST=${redis.service.host}
REDIS_PORT=${redis.service.port}
SAKERHETSTJANST_WS_CERTIFICATE_FILE=${application.dir}/certifikat/localhost.p12
SAKERHETSTJANST_WS_CERTIFICATE_TYPE=PKCS12
SAKERHETSTJANST_WS_TRUSTSTORE_FILE=${application.dir}/certifikat/truststore.jks
SAKERHETSTJANST_WS_TRUSTSTORE_TYPE=JKS
SPRING_PROFILES_ACTIVE=test,wc-loggtjanst-stub,testability-api,caching-enabled