app:
  aggregation:
    bulkSize: 5
    bulkTimeout: 60000
  certificate:
    file: ${application.dir}/certifikat/localhost.p12
    keyManagerPassword: password
    password: password
    truststoreFile: ${application.dir}/certifikat/truststore.jks
    truststorePassword: password
    type: PKCS12
  loggtjanst:
    logicalAddress: SE000000000000-0000

logging:
  structured:
    format:
      console: ''

ntjp:
  base:
    url: http://localhost:${dev.http.port}/stubs

server:
  port: 8010

spring:
  activemq:
    broker-url: tcp://localhost:61616?jms.nonBlockingRedelivery=true&jms.redeliveryPolicy.maximumRedeliveries=3&jms.redeliveryPolicy.maximumRedeliveryDelay=6000&jms.redeliveryPolicy.initialRedeliveryDelay=4000&jms.redeliveryPolicy.useExponentialBackOff=true&jms.redeliveryPolicy.backOffMultiplier=2
    password: activemqPassword
    user: activemqUser
  application:
    name: logsender-dev
  data:
    redis:
      host: localhost
      password: redis
      port: 6379

