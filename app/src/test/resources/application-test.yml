app:
  aggregation:
    bulkSize: 5
    bulkTimeout: 5000
  loggtjanst:
    logicalAddress: 1234-1234
  certificate:
    file: ${user.dir}/certifikat/localhost.p12
    keyManagerPassword: password
    password: password
    truststoreFile: ${user.dir}/certifikat/truststore.jks
    truststorePassword: password
    type: PKCS12
  queue:
    receiveLogMessageEndpoint: direct:receiveLogMessageEndpoint
    receiveAggregatedLogMessageEndpoint: direct:receiveAggregatedLogMessageEndpoint
    receiveAggregatedLogMessageDlq: direct:newAggregatedLogMessageDLQ

spring:
  activemq:
    broker-url: vm://localhost?broker.persistent=false